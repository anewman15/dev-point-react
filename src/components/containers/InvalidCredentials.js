const InvalidCredentials = () => (
  <div className="p-4 mx-auto w-80 rounded border border-red-400 bg-red-100 text-red-600 text-center">
    Invalid credentials. Please make sure the email/password is right
  </div>
);

export default InvalidCredentials;
